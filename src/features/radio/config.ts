/**
 * The only place to configure the AzuraCast station. Fill these in with your
 * self-hosted instance's values.
 *
 * - `baseUrl`: the origin of your AzuraCast install, no trailing slash
 *   (e.g. "https://radio.gain.online").
 * - `station`: the station's URL stub / short name / shortcode as it appears in
 *   AzuraCast (Station Profile -> "Station Short Name").
 */
export const radioConfig = {
  baseUrl: "https://radio.gain.place",
  station: "listen",
};
